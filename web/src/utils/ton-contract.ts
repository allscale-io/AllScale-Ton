// TON contract config and utilities for InvoiceManager

export const INVOICE_MANAGER_ADDRESS = 'EQCaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa'; // placeholder, update with real address

// Example: encode a payload for 'pay' (no params for demo)
export function encodePayInvoicePayload(): string {
  // For demo, just send an empty payload (real contract may require more)
  return '';
}

// Example: encode a payload for 'create invoice' (not used in minimal contract, but placeholder for future)
export function encodeCreateInvoicePayload(): string {
  // For demo, just send an empty payload
  return '';
} 